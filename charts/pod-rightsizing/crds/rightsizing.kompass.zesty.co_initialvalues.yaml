---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: initialvalues.rightsizing.kompass.zesty.co
spec:
  group: rightsizing.kompass.zesty.co
  names:
    kind: InitialValues
    listKind: InitialValuesList
    plural: initialvalues
    shortNames:
    - initialvalues
    singular: initialvalues
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InitialValues is the Schema for storing initial workload values.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired initial values.
            properties:
              containers:
                additionalProperties:
                  properties:
                    cpu:
                      description: CPU is the initial CPU value for the container.
                      type: number
                    memory:
                      description: Memory is the initial memory value for the container.
                      format: int64
                      type: integer
                  type: object
                description: Containers maps container name to container-scoped values.
                type: object
              workload:
                description: Workload stores initial workload-scoped values.
                properties:
                  cpuUtilization:
                    description: CPUUtilization is the initial CPU utilization percentage
                      configured for HPA.
                    format: int32
                    type: integer
                  kind:
                    description: Kind is the workload kind
                    minLength: 1
                    type: string
                  maxReplicas:
                    description: MaxReplicas is the initial maximum number of replicas
                      configured for HPA.
                    format: int32
                    type: integer
                  memoryUtilization:
                    description: MemoryUtilization is the initial memory utilization
                      percentage configured for HPA.
                    format: int32
                    type: integer
                  minReplicas:
                    description: MinReplicas is the initial minimum number of replicas
                      configured for HPA.
                    format: int32
                    type: integer
                  name:
                    description: Name is the workload name
                    minLength: 1
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - workload
            type: object
        type: object
    served: true
    storage: true
