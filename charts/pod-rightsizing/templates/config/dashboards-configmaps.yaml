---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kompass-pod-rightsizing-jobs-dashboard-config.name" . }}
  labels:
    grafana_dashboard: "1"
data:
  jobs-dashboard.json: |-
{{ .Files.Get "dashboards/jobs-dashboard.json" | indent 4 }}
