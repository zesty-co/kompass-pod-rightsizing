apiVersion: v1
kind: Service
metadata:
  name: {{ include "action-taker.name" . }}-metrics
  namespace: {{ .Release.Namespace }}
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: pod-rightsizing
spec:
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: pod-rightsizing
  ports:
    - name: https-metrics
      port: 8443
      targetPort: 8443
      protocol: TCP
